// Placeholder — API para envíos (se implementa en Etapa 2)
import { NextResponse } from 'next/server'

export async function GET() {
    return NextResponse.json({ message: 'Shipments API — Coming soon' })
}
