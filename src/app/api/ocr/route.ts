// Placeholder — API para OCR con IA (se implementa en Etapa 3)
import { NextResponse } from 'next/server'

export async function POST() {
    return NextResponse.json({ message: 'OCR API — Coming soon' })
}
