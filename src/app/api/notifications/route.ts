// Placeholder — API para notificaciones (se implementa en Etapa 4)
import { NextResponse } from 'next/server'

export async function POST() {
    return NextResponse.json({ message: 'Notifications API — Coming soon' })
}
